#
spring:
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  application:
    admin:
      enabled: false
      jmx-name: org.springframework.boot:type=Admin,name=SpringTreentityApplication

app:
  db-name: treentity
  db-host: localhost
  db-port: 5432
  db-username: postgres
  db-password: postgres
